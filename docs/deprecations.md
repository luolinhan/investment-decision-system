# Deprecations

更新日期：2026-04-26

本文件集中标记旧资产。除非另有迁移计划，不直接删除旧脚本，避免破坏 Windows 现有任务或历史回放。

## Deprecated: 外部编码与旧翻译增强资产

状态：deprecated，保留兼容。

范围包括：

- 旧外部编码工作流说明。
- 旧外部 worker 交付记录。
- 旧翻译增强 bat/ps1 包装脚本。
- 旧文档中把外部编码工具作为执行层的描述。

规则：

- 新 V3 代码、脚本、配置和文档不得继续把这些资产作为执行路径。
- 旧资产如果仍被 Windows 任务引用，先迁移到 `scripts/investment_job_runner.py` 或明确隔离，再删除。
- 新功能不新增相关环境变量或调度入口。

